<%= provide(:title, "Note") %>

<div class="col-md-12">
  <%= link_to "Back to notes", notes_path, class: "btn btn-primary" %>
  <% if current_user?(@note.user) %>
    <%= link_to "Edit", edit_note_path(@note), class: "btn" %>
    <%= link_to "Delete", @note, method: :delete, class: "btn", data: {confirm: "Are you sure?"} %>
  <% end %>
</div>

<div class="notes">
  <span class="timestamp">Posted: <%= time_ago_in_words(@note.created_at) %> ago
  Updated: <%= time_ago_in_words(@note.updated_at) %> ago</span>
  <div class="show">
    <%= simple_format @note.content %>
  </div>
</div>

<div class="col-md-12">
  <%= link_to "Back to notes", notes_path, class: "btn btn-primary" %>
  <% if current_user?(@note.user) %>
    <%= link_to "Edit", edit_note_path(@note), class: "btn" %>
    <%= link_to "Delete", @note, method: :delete, class: "btn", data: {confirm: "Are you sure?"} %>
  <% end %>
</div>

