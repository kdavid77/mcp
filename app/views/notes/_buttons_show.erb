<% if object.archived %>
  <%= link_to "Activate", note_activate_path(object), class: "btn btn-xs" %>
<% else %>
  <%= link_to "Archive", note_archive_path(object), class: "btn btn-xs" %>
<% end %> 

<%= link_to "Edit", edit_note_path(object), class: "btn btn-xs" if not object.archived %>

<%= link_to "Delete", object, method: :delete, class: "btn btn-xs", data: {confirm: "Are you sure?"} %>